# --- Core LangChain / LangGraph ---
langchain>=1.3.0
langgraph>=1.2.0
langchain-core>=0.3.0

# --- LLM provider (Gemini, per your reference code) ---
langchain-google-genai>=2.0.0

# --- Vector store for the RAG tool (pick ONE) ---
chromadb>=0.5.0
langchain-chroma>=0.1.4
langchain-community>=0.3.0
# qdrant-client>=1.11.0        # uncomment if using Qdrant instead of Chroma
# langchain-qdrant>=0.2.0

# --- Document loaders for PDF and Word policy files ---
pypdf>=4.3.0
docx2txt>=0.8

# Embeddings: use Gemini's own embedding model (models/text-embedding-004)
# via langchain-google-genai — no separate package needed, already included above.

# --- Env vars / config ---
python-dotenv>=1.0.0

# --- Demo wrapper ---
streamlit>=1.38.0

# --- Data handling for eval harness ---
pandas>=2.2.0

# --- Pydantic (structured output schemas) ---
pydantic>=2.8.0
langchain-text-splitters
requests
pyyaml
langchain-classic